#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int		ans[500001] = { 0, };

int		main()
{
	int N;
	scanf("%d", &N);

	for (int i = 0; i < N; i++)
		scanf("%d", &ans[i]);

	for (int i = N - 1; i >= 0; i--)
	{

	}

	for (int i = 0; i < N; i++)
	{

	}
}